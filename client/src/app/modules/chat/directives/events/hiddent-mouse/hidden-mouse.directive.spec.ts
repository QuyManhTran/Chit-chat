import { HiddenMouseDirective } from './hidden-mouse.directive';

describe('HiddenMouseDirective', () => {
  it('should create an instance', () => {
    const directive = new HiddenMouseDirective();
    expect(directive).toBeTruthy();
  });
});

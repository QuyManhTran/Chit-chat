import { HiddenClickDirective } from './hidden-click.directive';

describe('HiddenClickDirective', () => {
  it('should create an instance', () => {
    const directive = new HiddenClickDirective();
    expect(directive).toBeTruthy();
  });
});

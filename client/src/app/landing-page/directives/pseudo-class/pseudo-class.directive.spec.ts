import { PseudoClassDirective } from './pseudo-class.directive';

describe('PseudoClassDirective', () => {
  it('should create an instance', () => {
    const directive = new PseudoClassDirective();
    expect(directive).toBeTruthy();
  });
});

export enum MessageType {
    AUDIO = 'audio',
    TEXT = 'text',
    PHOTO = 'image',
    DOCUMENT = 'pdf',
    APPLICATION = 'application',
}

export enum Call {
    AUDIO = 'audio',
    VIDEO = 'video',
}
